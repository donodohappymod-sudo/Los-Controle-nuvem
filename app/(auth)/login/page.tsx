'use client';
import {FormEvent,useState} from 'react'; import {useRouter} from 'next/navigation';
export default function Login(){const router=useRouter();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e:FormEvent){e.preventDefault();setLoading(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok){setError(d.error||'Falha no login.');setLoading(false);return;}window.location.href='/dashboard';}return <main className="login"><section className="loginBox"><div className="loginBrand"><div className="crown">♛</div><div><b>LOS COLLECTOR</b><small style={{display:'block',color:'var(--muted)',fontSize:9,letterSpacing:'.14em'}}>COLLECT • ORGANIZE • MONITOR</small></div></div><div className="eyebrow">PRIVATE SAAS</div><h1 className="title">Acesso ao painel</h1><p className="sub" style={{marginBottom:22}}>Centralize fontes, biblioteca, diagnóstico, monitoramento e Studio.</p>{error&&<div className="error" style={{marginBottom:12}}>{error}</div>}<form className="form" onSubmit={submit}><label>E-mail<input className="input" type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)} required /></label><label>Senha<input className="input" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)} required /></label><button className="btn primary" disabled={loading}>{loading?'ENTRANDO…':'ENTRAR'}</button></form></section></main>}

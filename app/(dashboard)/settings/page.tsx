'use client';
import {useState} from 'react';
export default function Settings(){const [current,setCurrent]=useState('');const [next,setNext]=useState('');const [msg,setMsg]=useState('');async function save(){const r=await fetch('/api/settings/password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({current,next})});const d=await r.json();setMsg(r.ok?'Senha alterada com sucesso.':d.error||'Erro.');if(r.ok){setCurrent('');setNext('')}}return <><div className="hero"><div><div className="eyebrow">CONFIGURAÇÕES</div><h1 className="title">Segurança</h1><p className="sub">Altere a senha sem expor segredo no repositório.</p></div></div><div className="grid2"><div className="card"><h3>Alterar senha</h3><div className="form"><label>Senha atual<input className="input" type="password" value={current} onChange={e=>setCurrent(e.target.value)}/></label><label>Nova senha<input className="input" type="password" value={next} onChange={e=>setNext(e.target.value)}/></label><button className="btn primary" onClick={save}>SALVAR</button>{msg&&<div className={msg.includes('sucesso')?'success':'error'}>{msg}</div>}</div></div><div className="card"><h3>Ambiente</h3><p className="sub">Banco: PostgreSQL<br/>Processamento: servidor<br/>Monitoramento: job do Render<br/>Mídia: armazenamento persistente configurável</p></div></div></>}
